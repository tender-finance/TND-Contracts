# TND Contracts
Contracts for TND.

## Install Dependencies
If npx is not installed yet:
`npm install -g npx`

Install packages:
`npm i`

## Compile Contracts
`npx hardhat compile`

## Clean Contracts
`npx hardhat clean`

## Run Tests
`npx hardhat test`
